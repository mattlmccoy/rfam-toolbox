[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rfam-toolbox"
version = "1.0.0"
description = "Tools for Radio Frequency and Binder Jet Additive Manufacturing process analysis"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
    {name = "Matthew L. McCoy", email = "matthew.mccoy@gatech.edu"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "opencv-python>=4.5.0",
    "numpy>=1.20.0",
    "matplotlib>=3.4.0",
    "pandas>=1.3.0",
    "scikit-image>=0.18.0",
]

[project.optional-dependencies]
pdf = ["pdf2image>=1.14.0"]
dev = [
    "pytest>=6.0",
    "pytest-cov>=2.0",
    "black>=21.0",
    "flake8>=3.9",
    "build",
    "twine",
]

[project.scripts]
rfam-toolbox = "rfam_toolbox.launcher:main"

[project.urls]
Homepage = "https://github.com/mattlmccoy/rfam-toolbox"
Repository = "https://github.com/mattlmccoy/rfam-toolbox"
Issues = "https://github.com/mattlmccoy/rfam-toolbox/issues"

[tool.setuptools.packages.find]
include = ["rfam_toolbox*"]

[tool.setuptools.package-data]
rfam_toolbox = ["**/*.png", "**/*.pdf"]
